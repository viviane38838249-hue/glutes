(function(){"use strict";function startCountdowns(){let totalSeconds=14*60+59;const nodes=document.querySelectorAll("[data-countdown]");function render(){const m=Math.floor(totalSeconds/60);const s=totalSeconds%60;const text=String(m).padStart(2,"0")+":"+String(s).padStart(2,"0");nodes.forEach((el)=>(el.textContent=text));}
render();setInterval(()=>{if(totalSeconds>0){totalSeconds-=1;render();}},1000);}
function initFaq(){document.querySelectorAll(".faq-btn").forEach((btn)=>{btn.addEventListener("click",()=>{const answer=btn.nextElementSibling;const isOpen=btn.getAttribute("aria-expanded")==="true";btn.setAttribute("aria-expanded",String(!isOpen));answer.classList.toggle("open",!isOpen);});});}
const NOMES=["María","Ana","Sofía","Isabella","Camila","Valentina","Gabriela","Daniela","Fernanda","Lucía","Carmen","Rosa","Elena","Patricia","Adriana","Juliana","Mariana","Paula"];const LOCAIS=["Ciudad de México, MX","Bogotá, CO","Buenos Aires, AR","Lima, PE","Santiago, CL","Quito, EC","Guatemala, GT","San José, CR","Panamá, PA","Montevideo, UY","Caracas, VE","Asunción, PY","La Paz, BO","Santo Domingo, DO"];function initSocialProof(){const toast=document.getElementById("social-proof-toast");if(!toast)return;const nameEl=toast.querySelector("[data-name]");const ageEl=toast.querySelector("[data-age]");const locEl=toast.querySelector("[data-location]");function fire(){const name=NOMES[Math.floor(Math.random()*NOMES.length)];const loc=LOCAIS[Math.floor(Math.random()*LOCAIS.length)];const age=Math.floor(Math.random()*39)+25;nameEl.textContent=name;ageEl.textContent=age;locEl.textContent=loc;toast.classList.add("show");setTimeout(()=>toast.classList.remove("show"),5000);}
setTimeout(fire,3000);setInterval(fire,15000+Math.random()*10000);}
function initScrollToCheckout(){document.querySelectorAll("[data-scroll-checkout]").forEach((el)=>{el.addEventListener("click",(e)=>{const target=document.getElementById("checkout-section");if(target){e.preventDefault();target.scrollIntoView({behavior:"smooth"});}});});}
document.addEventListener("DOMContentLoaded",()=>{startCountdowns();initFaq();initSocialProof();initScrollToCheckout();});})();